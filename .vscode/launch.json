{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch Chrome against localhost",
        "type": "chrome",
        "request": "launch",
        "url": "http://localhost:5500", // Replace with your live server URL if it's different.
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  